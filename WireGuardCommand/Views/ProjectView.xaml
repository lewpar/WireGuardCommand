<UserControl x:Class="WireGuardCommand.Views.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WireGuardCommand.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{Binding Project.Name}"></TextBlock>
                <TextBlock Text="{Binding Config.Test}"></TextBlock>
                <Button Command="{Binding MakeChangesCommand}">Make Changes</Button>
            </StackPanel>
            <Button Command="{Binding TryCloseProjectCommand}">Close Project</Button>
        </Grid>

        <Grid>
            <Grid.Background>
                <SolidColorBrush Color="Black" Opacity="0.5"/>
            </Grid.Background>
            <Grid.Style>
                <Style TargetType="{x:Type Grid}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsClosingWithUnsavedChanges}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock>You have unsaved changes, are you sure you want to close the project?</TextBlock>
                <StackPanel Orientation="Horizontal">
                    <Button Command="{Binding CloseProjectCommand}">Yes</Button>
                    <Button Command="{Binding ReturnToProjectCommand}">No</Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
